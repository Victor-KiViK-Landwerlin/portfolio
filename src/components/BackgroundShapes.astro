---
// Composant pour les formes géométriques en arrière-plan
---

<div class="fixed inset-0 pointer-events-none overflow-hidden -z-10">
  <!-- Bande verticale centrale -->
  <div class="absolute left-1/2 top-0 w-12 h-full bg-kivik-light/30 transform -translate-x-1/2"></div>
  
  <!-- Diagonales -->
  <div class="absolute top-0 left-0 w-96 h-96 bg-kivik-light/20 transform -rotate-45 -translate-x-48"></div>
  <div class="absolute top-0 right-0 w-96 h-96 bg-kivik-light/20 transform rotate-45 translate-x-48"></div>
  <div class="absolute bottom-0 left-1/4 w-96 h-96 bg-kivik-light/20 transform rotate-45 translate-y-48"></div>
  <div class="absolute top-1/3 right-0 w-64 h-96 bg-kivik-light/15 transform -rotate-12"></div>
</div>

<style>
  /* Animations subtiles pour les formes */
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }
  
  div > div {
    animation: float 20s ease-in-out infinite;
  }
  
  div > div:nth-child(2) {
    animation-delay: -5s;
  }
  
  div > div:nth-child(3) {
    animation-delay: -10s;
  }
  
  div > div:nth-child(4) {
    animation-delay: -15s;
  }
</style>
